# =============================================================================
# Wikiio Docker Compose — 后端服务编排（前端已迁至 EdgeOne Pages 静态托管）
#
# 服务清单（6 项）:
#   1. postgres      — PostgreSQL 16 数据库
#   2. redis         — Redis 7 缓存 + Celery Broker
#   3. backend       — FastAPI 应用 (内部 :8000，不暴露到宿主机)
#   4. celery-worker — Celery 异步任务执行器
#   5. celery-beat   — Celery 定时任务调度器
#   6. nginx         — Nginx 反代 + SSL 终端 (:80/:443)
# =============================================================================
services:
  postgres:
    image: postgres:16-alpine
    container_name: wikiio-postgres
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: wikiio
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: wikiio_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wikiio -d wikiio_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    container_name: wikiio-redis
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wikiio-backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      DATABASE_URL: ${DB_URL}
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: production
      CORS_ORIGINS: "https://wikiio.verniy.site,http://localhost:5173,http://127.0.0.1:5173"
    volumes:
      - logs:/logs
    expose:
      - "8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - app-network

  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wikiio-celery-worker
    restart: unless-stopped
    command: celery -A app.crawler.scheduler worker --loglevel=info -P solo
    env_file: ./backend/.env
    environment:
      DATABASE_URL: ${DB_URL}
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: production
    volumes:
      - logs:/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    networks:
      - app-network

  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wikiio-celery-beat
    restart: unless-stopped
    command: celery -A app.crawler.scheduler beat --loglevel=info
    env_file: ./backend/.env
    environment:
      DATABASE_URL: ${DB_URL}
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: production
    volumes:
      - logs:/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    container_name: wikiio-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/wikiio.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./nginx/certbot-challenge:/var/www/certbot:ro
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  postgres_data:
    name: wikiio-postgres-data
  logs:
    name: wikiio-logs

networks:
  app-network:
    name: wikiio-app-network
    driver: bridge
